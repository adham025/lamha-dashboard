# Copy to .env for local dev. On Vercel, set these in
# Project → Settings → Environment Variables (NOT committed).
# Anon key only — never put the service_role key here; it would ship to the browser.
VITE_SUPABASE_URL=https://sqfaiygsfphheoauxrpu.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
